"""Initialization strategy helpers — hmmlearn handles init internally via init_params.

This module is a placeholder for Phase 2+ custom initialization (e.g. equal-prior
seeding or deterministic k-means warm starts). For now hmmlearn's built-in k-means
initialization is used by default (init_params='stmc').
"""
